name: CI

on:
  push:
  pull_request:

jobs:
  qphysics-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Run QPhysics regression tests
        run: node tests/qphysics.test.js
